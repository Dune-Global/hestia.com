/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['greensupermarketstoreacc.blob.core.windows.net', 'example.com', 'www.nsbm.ac.lk'],
  },
};

export default nextConfig;