export enum PropertyType {
  House = "house",
  Apartment = "apartment",
  Hostel = "hostel",
}

export enum ShareType {
  Entire = "entire",
  Private = "private",
  Shared = "shared",
}

export enum PropertyStatus {
  Available = "available",
  Unavailable = "unavailable",
}
