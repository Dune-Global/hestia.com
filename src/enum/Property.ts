export enum PropertyType {
  House = "house",
  Apartment = "apartment",
  Hostel = "hostel",
}

export enum ShareType {
  Entire = "entire",
  Private = "private",
  Shared = "shared",
}

export enum PropertyStatus {
  Approved = "approved",
  Pending = "pending",
  rejected = "rejected",
  Unavailable = "unavailable",
}
